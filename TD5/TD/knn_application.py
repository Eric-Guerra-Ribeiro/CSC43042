def my_parametrization(d: int, nb_train_data: int) -> (int, int, str, float, int):
    # Define good values for the variables that are returned.
    pass

    return (k, projection_dim, type_sample, threshold, seed)
